---
import EmptyState from './EmptyState.astro';
import ProductCard from './ProductCard.astro';
interface Product { name: string; description: string; href?: string; price?: string; priceState: 'observed' | 'estimate' | 'unavailable' | 'stale'; observedAt?: string; source?: string }
interface Props { title: string; products: Product[] }
const { title, products } = Astro.props;
---
<section class="hb-product-shelf" aria-labelledby="product-shelf-title">
  <h2 id="product-shelf-title">{title}</h2>
  {products.length > 0 ? <div>{products.map((product) => <ProductCard {product} />)}</div> : <EmptyState title="No verified products" message="No evidence-backed product records are available for this shelf." />}
</section>
