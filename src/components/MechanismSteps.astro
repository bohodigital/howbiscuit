---
const { steps } = Astro.props as { steps: Array<{ title: string; body: string }> };
---

<ol class="hb-mechanism">
  {steps.map((step, index) => (
    <li>
      <span>{String(index + 1).padStart(2, '0')}</span>
      <div><h3>{step.title}</h3><p>{step.body}</p></div>
    </li>
  ))}
</ol>
